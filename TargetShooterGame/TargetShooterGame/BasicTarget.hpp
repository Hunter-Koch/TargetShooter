#include <SFML/Graphics.hpp>
#include "Target.hpp"
/*
class BasicTarget : public Target
{
private:
	int pointValue;
	bool destroyed;


public:
	
	//= sf::Texture("Assets/other/Error_texture.png"
	//= sf::Vector2f(1, 1)

	BasicTarget(const sf::Vector2f& pos = sf::Vector2f(1, 1))
		: pointValue(5), destroyed(false)
	{
		this->setPosition(pos);
		sf::Texture texture = sf::Texture("Assets/targets/Basic/Target_32x32.png");
		this->setTexture(texture);
	}
	
	
	
	std::string toString();
	void update() override;
};
*/